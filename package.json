{
  "name": "boltroute-monorepo",
  "private": true,
  "scripts": {
    "dev:dashboard": "npm --prefix apps/dashboard run dev",
    "build:dashboard": "npm --prefix apps/dashboard run build",
    "test:dashboard": "npm --prefix apps/dashboard run test:history && npm --prefix apps/dashboard run test:auth-guard && npm --prefix apps/dashboard run test:overview && npm --prefix apps/dashboard run test:account-purchases",
    "dev:website": "npm --prefix apps/website run dev",
    "build:website": "npm --prefix apps/website run build",
    "lint:website": "npm --prefix apps/website run lint",
    "content:website": "npm --prefix apps/website run content:build"
  }
}
